function getPrimeNumber(max){
var prime =[];
var newPrime = [];
var num;
var i;
var q = math.floor(Math.srt(n));
for (num = 2; num <= max; ++num) 
{
	if (num%) {
	prime.push(num)
	}
	if (num%2==0){
	prime.push(num)
	}
	for (var i = 2;  i < num; i++) {

      if (num % i !== 0 ) {
      prime.push(getPrimeNumber)
      }
}